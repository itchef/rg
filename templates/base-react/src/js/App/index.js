import React, { PureComponent } from 'react';
import "./index.scss";

export default class App extends PureComponent {
    render() {
        return (
            <div className="app__wrapper">Base App Created.</div>
        );
    }
}
